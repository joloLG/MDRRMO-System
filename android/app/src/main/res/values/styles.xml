<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <!-- Full screen settings -->
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowBackground">@color/splashBackground</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <!-- 
        Splash Screen Theme for Android 12+ (API 31+)
        Uses the new SplashScreen API
    -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <!-- Splash screen background color - White -->
        <item name="windowSplashScreenBackground">@color/splashBackgroundWhite</item>
        
        <!-- The splash screen icon/animation - use the Bulan logo -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_logo</item>
        
        <!-- Animation duration (max 1000ms) -->
        <item name="windowSplashScreenAnimationDuration">300</item>
        
        <!-- Icon background color -->
        <item name="windowSplashScreenIconBackgroundColor">@color/splashBackgroundWhite</item>
        
        <!-- Status bar and navigation bar settings -->
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowNoTitle">true</item>
        
        <!-- The theme to use after splash screen -->
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
    </style>

    <!-- Legacy Splash Theme for Android 11 and below -->
    <style name="AppTheme.Launch" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@layout/splash_screen_layout</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowNoTitle">true</item>
    </style>

</resources>